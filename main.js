function multiplyMat(mat1, mat2)
{
    if(mat1.length == mat2.length)
    {
        //The sizes might just be the same
        let result, sum = 0;
        for(let i =0)
    }
}
gl_Position = vec4(x,y,z,w)
new Vector =vec4(r*x, r*y, r*z, w);